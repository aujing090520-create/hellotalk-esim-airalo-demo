# HelloTalk eSIM 研究与 Demo 审计工作稿

> 本文用于沉淀竞品研究、服务端集成边界和现有 Demo 审计结论，不是正式林凡 PRD，不作为 Demo、研发或测试的唯一规则源。正式规则以 `PRD_HelloTalk_eSIM_MVP.md` 为准。

## 版本管理

| 编号 | 日期 | 动作 | 内容 | 操作人 | 平台 |
|---|---|---|---|---|---|
| 1 | 2026.08.06 | 需求初稿 | 基于现有 Demo、旅行 eSIM 公开用户流程与首期履约渠道/Stripe 集成边界反推 MVP；Demo 不是需求真相。 | 林凡 | iOS & Android，服务端 |

## 需求相关资源

| 资源 | 链接 | 负责人 |
|---|---|---|
| 现有 Demo | 本地项目 `hellotalk-esim-airalo-demo` | 产品 / 研发 |
| 旅行 eSIM 用户流程参考 | [Airalo Help Center](https://www.airalo.com/help) | 产品 |
| 首期履约渠道 API 参考 | [Airalo Partner Platform](https://developers.partners.airalo.com) | 服务端 |
| 支付参考 | [Stripe Checkout](https://docs.stripe.com/payments/checkout)；[Stripe Webhooks](https://docs.stripe.com/webhooks) | 服务端 |
| 设计稿 | 待补充 | 设计 |
| 产品交互稿 | 待补充 | 产品 |
| 测试用例 | 待补充 | 测试 |
| 多语言 | 待补充 | 本地化 |
| 数据上报 | 待补充 | 数据 |
| 上线后数据分析 | 待补充 | 数据 |

## 需求背景

HelloTalk 计划在 App 内销售旅行 eSIM。服务端通过履约渠道适配层接入首期合作渠道，支付由 HelloTalk 自有 Stripe 账户承接；Airalo Partner Platform 仅作为首期技术参考。

现有 Demo 已展示目的地、套餐、购买、安装、连接、用量和加购的主线，但其中部分状态为前端本地模拟，不能直接作为生产逻辑。本工作稿以公开可验证的旅行 eSIM 用户流程为参照，定义首个可上线 MVP 的行为边界。

不建设自有 eSIM 运营后台，不恢复个人资料 Tab、推荐、HelloMoney、已保存银行卡、订单页或通知偏好。运营操作分别由履约渠道平台、Stripe Dashboard 和 HelloTalk 服务端配置承担。

## 产品目标

让 HelloTalk 用户可以在出行前完成目的地选择、套餐购买和 eSIM 获取，并在抵达后按明确指引安装与连接。

保证支付成功、履约渠道交付、eSIM 可安装、用量展示和加购资格由服务端真实状态驱动，不把客户端演示状态当作订单事实。

在不展示银行卡和不自建运营后台的前提下，提供可追踪的支付、履约、异常处理与支持入口。

## 业务流程

```text
进入商店
-> 搜索或选择本地 / 区域 / 全球目的地
-> 查看套餐、覆盖范围、有效期起算策略和支持网络
-> 设备兼容性提示
-> HelloTalk 服务端创建价格快照与 Stripe Checkout Session
-> Stripe 支付结果由 Webhook 确认
-> HelloTalk 服务端通过履约渠道适配器创建订单并轮询 / 接收交付状态
-> 我的 eSIM 展示“交付中”或可安装 eSIM
-> 按服务端返回的安装方式完成安装
-> 到达后按系统设置指引连接，刷新真实状态和用量
-> 符合服务端返回资格时购买 Top up
-> 异常进入帮助与支持 / 退款处理
```

## 需求概览

| 需求名称 | 概述 | 优先级 | 平台 |
|---|---|---|---|
| 用户端购买与交付 | 从目的地浏览到 Stripe 支付、履约交付、安装和连接的完整链路。 | P0 | iOS & Android，服务端 |
| 用量、加购与售后 | 以履约渠道真实数据展示 eSIM 状态、可加购包和支持入口。 | P0 | iOS & Android，服务端 |
| 履约与支付集成 | 使用履约渠道适配层和 HelloTalk Stripe，服务端负责订单、幂等、回调、退款和对账。 | P0 | 服务端 |
| Demo 评审追踪 | 将当前 Demo 的评审编号迁移到本 PRD，正常模式不暴露评审元素。 | P1 | Demo |

## 需求详情

| 功能 | UI/layout | 描述 | 备注 |
|---|---|---|---|
| FR-001 用户端范围与入口 | 商店 Tab；我的 eSIM Tab | 1. 用户端只保留“商店”和“我的 eSIM”两个一级 Tab。<br>2. 不展示账户资料、收件箱、忠诚计划、HelloMoney、推荐好友、已保存银行卡、订单、对公业务、语言设定和通知偏好。<br>3. 用户历史购买和服务状态统一在“我的 eSIM”中呈现，不新增独立订单 Tab。<br>4. 付款信息只在 Stripe Hosted Checkout 中处理，HelloTalk 客户端不采集或保存卡号。 | Demo 已符合主导航；需持续防止已删除能力回流。小助手未调用，已按研发视角自检。 |
| FR-002 目的地发现与搜索 | 商店首页；选择目的地页 | 1. 首页提供目的地搜索、本地、区域、全球套餐入口及目的地最低展示价。<br>2. 首页 eSIM 宣传轮播仅说明产品作用，不提供跳转或购买 CTA。<br>3. 搜索同时匹配国家、地区和全球套餐；无结果时展示多语言“暂未找到该目的地”。<br>4. 空搜索保留可售目的地目录；用户有搜索记录时，在输入框下展示最近搜索。记录按最近选择排序、去重并最多保留 5 条。<br>5. 区域和全球套餐必须展示实际覆盖国家/地区列表入口，不得只展示“覆盖 N 个目的地”的静态文案。<br>6. 目的地、覆盖范围、最低价和可售状态均来自服务端同步后的目录；无可售套餐时展示不可购买状态。 | 第一期交易入口为搜索和目的地分类。 |
| FR-003 套餐详情与价格 | 目的地页；套餐详情页 | 1. 每个套餐展示流量、有效期、价格、币种、覆盖范围、网络/运营商信息、是否可加购及有效期起算说明。<br>2. “不限流量”套餐必须展示服务端返回的公平使用、限速或其他适用限制；未返回时不自行承诺无限高速流量。<br>3. 有效期文案按套餐返回的 activation policy 展示，例如安装后起算或首次连接支持网络后起算；不得按前端固定枚举覆盖履约渠道规则。<br>4. 价格展示使用服务端返回的售卖币种和金额；进入结算前重新校验套餐可售性与价格。<br>5. 套餐停售、价格变化或目录同步失败时不可继续支付，并返回目的地页提示刷新。 | 当前 Demo 已按目的地绑定 Catalog；“不限流量”限制、价格锁定和动态起算规则缺失。 |
| FR-004 设备兼容性与购买前提示 | 新手引导；套餐详情；兼容性提示页 | 1. 首次浏览可跳过兼容性检查，不因未检查阻断目的地和套餐浏览。<br>2. 购买前提示设备需支持 eSIM 且处于运营商解锁状态，并提供兼容设备查询入口。<br>3. 已识别为不兼容设备时，用户可取消购买或明确选择为另一台兼容设备购买；后者必须提示安装需在目标设备上完成。<br>4. 兼容性检查失败、型号未收录或无法读取设备信息时展示“暂无法确认”，不得误判为不支持。<br>5. eSIM 安装后不得承诺可转移到另一台设备；具体限制以服务端返回的安装规则为准。 | 当前 Demo 使用用户自报“支持/不支持”，只能作为演示；生产需接入兼容设备数据或人工查询页。 |
| FR-005 创建结算与跳转 Stripe | 确认订单页；Stripe Hosted Checkout | 1. 用户点击付款后，客户端请求 HelloTalk 服务端创建内部订单和 Stripe Checkout Session。<br>2. 服务端在订单中固化用户、套餐、履约渠道套餐 ID、售卖价格、币种、税费/折扣、幂等键和创建时间；客户端不能自行生成订单或履约渠道 ID。<br>3. 跳转 Stripe 前展示套餐、应付金额和多语言“付款将由 Stripe 安全处理”；支付方式由当前 Checkout Session 的地区、币种和 Stripe 账户配置决定，支付宝仅在符合资格且已启用时可用。<br>4. 取消 Stripe Checkout 时返回确认订单，订单保留为未支付/已取消，不生成 eSIM。<br>5. 客户端回跳仅用于恢复页面，不得以回跳参数判定支付成功。 | 当前 Demo 的 Stripe 承接页可保留为演示，但“支付”按钮仅能模拟，不能作为生产支付实现。 |
| FR-006 支付确认与 eSIM 交付 | 支付返回页；我的 eSIM 交付中状态 | 1. 仅在服务端验证 Stripe Webhook 事件、签名和订单金额后，将订单标记为支付成功。<br>2. 支付成功后由服务端创建或查询履约订单；同一内部订单重复回调、重试或用户刷新只能产生一次履约渠道下单。<br>3. 未完成交付时，“我的 eSIM”展示多语言“交付中”和多语言“正在准备 eSIM”，并提供刷新和联系支持入口。<br>4. 返回可安装 eSIM 后，服务端保存履约渠道订单 ID、eSIM ID、ICCID、安装数据引用和履约时间；客户端仅读取脱敏后的必要信息。<br>5. 支付成功但下单/履约失败时，不创建可安装 eSIM；订单进入人工处理或自动退款流程，并告知用户处理状态。<br>6. 付款成功页不得承诺“已购买成功且可安装”，除非该订单已获得履约渠道可安装结果。 | 当前 Demo 错误地在客户端支付点击后直接生成履约订单/eSIM/ICCID，必须改为服务端异步履约。 |
| FR-007 我的 eSIM 与订单状态 | 我的 eSIM 列表；eSIM 详情 | 1. 列表按最近交付/最近更新排序，展示目的地、套餐、可读状态、剩余流量或交付状态。<br>2. 支持至少“等待付款、交付中、待安装、安装指引可用、使用中、流量不足、已过期、处理异常”状态；客户端状态由内部订单与服务状态映射，不直接复用履约渠道原始枚举。<br>3. 每张 eSIM 详情展示套餐、有效期、流量、安装入口、最后同步时间和支持入口。<br>4. 用户可手动刷新详情；刷新失败保留最近一次成功数据并展示多语言“暂时无法更新，请稍后重试”。<br>5. 订单事实、履约渠道履约事实和用户已查看/已打开安装指引等客户端行为必须分开存储。 | 当前 Demo 有 eSIM 生命周期页面；需替换本地状态机为服务端映射状态。 |
| FR-008 eSIM 安装 | eSIM 详情；安装方式页 | 1. 仅展示服务端返回且当前可用的安装方式，例如应用内安装、二维码或手动安装；不可默认展示三种方式。<br>2. 应用内安装仅在当前系统和服务端返回可用的 direct-install 数据时展示；否则降级为二维码或手动安装。<br>3. 二维码、SM-DP+ 地址、激活码和安装链接属于敏感交付数据，客户端按需请求、避免日志和截图泄露，并在页面离开后不写入本地持久化。<br>4. 用户打开安装指引后可返回详情继续查看；安装流程中断时保留同一张 eSIM，不重复创建订单。<br>5. 用户移除 eSIM、系统报错或尝试再次安装时展示当前限制与支持入口，不承诺可重新安装。 | 当前 Demo 的固定演示二维码和手动激活码不能用于生产；“已安装”不应由前端按钮单独确认。 |
| FR-009 抵达后连接与故障引导 | eSIM 详情；连接指引；帮助与支持 | 1. 连接引导说明在系统设置中开启 eSIM 线路、将蜂窝数据切换到 eSIM、开启数据漫游；按 iOS/安卓分别提供系统级操作说明。<br>2. 当套餐要求 APN 或手动网络选择时，只展示服务端返回的该套餐指引；未返回时不展示虚构配置。<br>3. 用户完成系统设置后可点击刷新连接状态或用量，客户端不得仅因本地勾选项而把 eSIM 标记为使用中。<br>4. 无法连接时按“设备兼容/安装状态/数据漫游/APN/网络覆盖”顺序提供排查和支持入口。<br>5. 仅在服务端返回可识别状态时显示已启用、已连接等服务状态；否则使用“请完成设置后刷新”。 | 当前 Demo 本地复选框会直接把 eSIM 改为 active，和真实系统状态不一致。 |
| FR-010 有效期、用量与状态刷新 | eSIM 详情；我的 eSIM 卡片 | 1. 有效期开始和结束时间以服务端返回状态或套餐 activation policy 计算结果为准，并展示最后更新时间。<br>2. 若履约渠道支持用量查询，展示剩余流量、已用流量或不限流量状态；数据延迟时展示最近同步时间。<br>3. 若某套餐/履约渠道不提供实时用量，不展示伪造的剩余流量，改为多语言“暂不支持实时用量查询”。<br>4. 流量不足、过期、暂停和异常状态由服务端刷新或履约渠道回调触发；客户端不得提供“模拟流量不足/模拟过期”生产入口。<br>5. 到期后保留历史 eSIM 详情和支持入口，不自动删除用户记录。 | 当前 Demo 的流量和到期均为手工模拟，不能复用为生产规则。 |
| FR-011 Top up 加购 | eSIM 详情；可加购包列表；确认加购；Stripe Hosted Checkout | 1. 仅当服务端依据当前 eSIM 的履约标识/ICCID 查询并返回可加购包时，展示多语言“加购流量”入口。<br>2. 加购包以服务端返回的可售列表为准，不得用“同一目的地 Catalog 下全部套餐”替代。<br>3. 加购可在使用中、流量不足或过期时是否可售，以服务端返回的资格和套餐规则为准；客户端不写死仅限流量不足/过期。<br>4. 加购支付、Webhook、履约渠道履约复用 FR-005 和 FR-006；支付成功后不得直接覆盖原套餐的流量、起算时间或到期时间。<br>5. 加购成功后由服务端根据服务端返回的 package history/用量状态更新详情，并显示多语言“加购正在生效”或最终状态。 | 当前 Demo 的加购资格和结果处理均不正确，是 MVP P0 修复项。 |
| FR-012 帮助、退款与异常告知 | 帮助与支持页；订单异常页 | 1. 提供安装、连接、用量、加购和付款问题的帮助入口，并可携带内部订单 ID/eSIM ID 进入客服工单。<br>2. 退款申请由 HelloTalk 售后规则决定；用户端不直接调用履约渠道或 Stripe 退款接口。<br>3. 支付成功但未交付、重复扣款、套餐明显不符等异常必须展示订单处理状态和支持入口。<br>4. Stripe 退款、履约渠道退款/取消和用户退款结果由服务端统一同步；退款成功后不再展示可用安装入口。<br>5. 不新增独立收件箱或通知偏好；必要状态在“我的 eSIM”和订单详情内可见。 | 当前 Demo 只有静态帮助提示，缺售后工单、退款和异常订单状态。 |

## 现存 Demo 审计

| 模块 | 审计结论 | 对应 PRD | 处理 |
|---|---|---|---|
| 商店、目的地、搜索、本地/区域/全球目录 | 基础信息架构可保留，但区域/全球缺覆盖明细和目录失效处理。 | FR-002 | 补页面和服务端目录来源。 |
| 套餐详情 | 已展示套餐基础字段，但不限流量限制、价格锁定、动态起算规则不足。 | FR-003 | 保留布局，改由真实套餐字段驱动。 |
| 设备兼容性 | 自报状态可作为 Demo，不可作为生产判断。 | FR-004 | 改为兼容设备查询/无法确认状态。 |
| Stripe 结算 | 演示页可保留；客户端生成成功订单不能保留。 | FR-005，FR-006 | 接入 Checkout Session、Webhook 和服务端幂等。 |
| eSIM 交付 | 客户端在支付后立即生成 providerOrderId、eSIM ID 和 ICCID，逻辑错误。 | FR-006 | 删除客户端造数，增加交付中与履约失败状态。 |
| 安装与连接 | 固定三种安装方式、固定手动码和本地勾选即 active 均不可信。 | FR-008，FR-009 | 依据履约渠道交付数据动态展示；用刷新替代本地改状态。 |
| 用量和过期 | 目前依赖“模拟流量不足/模拟过期”。 | FR-010 | 生产隐藏模拟工具，服务端同步数据。 |
| Top up | 目前按同目录套餐展示，并重置当前 eSIM 套餐/有效期，逻辑错误。 | FR-011 | 按 eSIM 查询可加购包和履约渠道履约结果更新。 |
| 帮助和退款 | 缺少真实工单、订单异常与退款状态。 | FR-012 | 补支持入口和服务端售后状态。 |
| 自建后台 | 已从 Demo 删除，方向正确。 | FR-013 至 FR-018 | 不恢复自建运营控制台。 |
| Linfan Review | 仍引用旧 PRD 的 FR-A01 至 FR-A05，无法追踪本 PRD。 | FR-019 | Demo 修订时迁移评审编号并补充新规则状态。 |

## 后台

| 功能 | UI/layout | 描述 | 备注 |
|---|---|---|---|
| FR-013 履约渠道目录同步与售卖配置 | 不建设自有后台；服务端任务与受控配置 | 1. 业务归属：履约渠道目录同步。<br>2. 服务端按约定频率同步目的地、套餐、覆盖范围、网络、价格、币种、activation policy、安装方式和可加购能力。<br>3. 参数：售卖地区、售卖币种、上架白名单、最低客户端版本，默认不展示未完整同步或不可售套餐。<br>4. 配置缺失、目录读取失败或字段校验失败时保留最近成功目录并停止新订单创建。 | 不建设运营人员编辑套餐的控制台；套餐真相来自服务端可售快照。 |
| FR-014 内部订单与 Stripe Webhook | 不建设自有后台；服务端订单服务 | 1. 业务归属：HelloTalk 商业化订单。<br>2. 参数：内部订单状态、Stripe Checkout Session ID、Payment Intent ID、金额、币种、幂等键、用户 ID，均由服务端写入；支付宝仅在当前会话符合资格且 Stripe 账户已启用时可用。<br>3. Stripe Webhook 必须校验签名、事件去重和金额/币种一致性；无效事件默认不改变订单状态。<br>4. 支付状态、退款状态和履约状态分别存储，避免支付成功被误当成交付成功。 | 根据研发视角自检补充：支付回跳不可替代 Webhook。 |
| FR-015 履约渠道下单、履约与敏感交付数据 | 不建设自有后台；服务端履约整合 | 1. 业务归属：履约渠道订单与 eSIM 交付。<br>2. 参数：履约渠道套餐 ID、履约订单 ID、eSIM ID、ICCID、履约状态、安装数据引用、最近同步时间。<br>3. 支付成功后按内部订单幂等创建履约订单；超时、重试和重复回调默认不重复发货。<br>4. 交付数据加密保存并按用户权限短时下发；日志、埋点和客服页面默认脱敏。<br>5. 履约渠道不可用时将订单标记为交付异常并进入退款/人工处理，不向客户端伪造可安装 eSIM。 | 根据研发视角自检补充：交付数据属于高敏感内容。 |
| FR-016 eSIM 用量、状态与 Top up 查询 | 不建设自有后台；服务端同步任务 | 1. 业务归属：eSIM 生命周期。<br>2. 参数：服务状态、用量、有效期、可加购包列表、数据更新时间，默认展示最近一次成功数据。<br>3. 按 eSIM ID/ICCID 查询可加购包，未返回资格时默认不展示加购入口。<br>4. 用量查询失败时不写入零流量或已过期；客户端展示刷新失败状态。<br>5. Top up 履约完成后按服务端返回的 package history/状态更新，不覆盖原套餐事实。 | 当前 Demo 本地状态机只可作为评审模拟，不能作为服务端真相。 |
| FR-017 退款、取消、对账与人工处理 | 不建设自有后台；Stripe Dashboard、履约渠道平台、内部工单 | 1. 业务归属：HelloTalk 售后与财务。<br>2. 参数：退款原因、退款状态、履约渠道取消/退款或贷项结果、Stripe Refund ID、处理人、处理时间，默认不自动退款。<br>3. 支付成功未交付、履约渠道明确失败和重复扣款进入预设处理策略；其他退款按 HelloTalk 售后规则进入人工审核。<br>4. 每日对账 Stripe 收款、内部订单、履约成本和结算调整；差异订单进入人工处理队列。<br>5. 退款完成后关闭对应 eSIM 的安装/加购入口，并保留用户可见的历史说明。 | 不在 Demo 中模拟财务后台；上线前需确认退款 SLA 和责任人。 |
| FR-018 监控、灰度与版本兼容 | 不建设自有后台；服务端监控与远程配置 | 1. 业务归属：发布与稳定性。<br>2. 参数：eSIM 入口开关、可售国家/套餐白名单、支付开关、最低客户端版本，默认关。<br>3. 配置缺失或读取失败时按照默认关处理，已完成订单和我的 eSIM 详情不受新入口开关影响。<br>4. 支付成功率、履约成功率、安装指引打开率、连接/用量刷新失败率超过阈值时告警并支持下架新购入口。<br>5. 需要备注，最低客户端版本以上生效；低版本只展示已有 eSIM 的必要详情和支持入口。 | 根据研发视角自检补充：灰度开关不应影响已交付用户。 |
| FR-019 Demo 评审映射 | Demo Review 模式 | 1. 正常模式不展示 FR 编号、评审面板、连线或模拟控制。<br>2. Review 模式中，每个核心页面和关键异常状态可从 FR-002 至 FR-012 回到对应规则。<br>3. 规则点击可定位页面或状态；页面编号可回到规则卡。<br>4. 演示专属的支付成功、流量不足和过期状态必须标记为“模拟状态”，不得表达为已接入真实支付或履约服务。 | 当前 Review 仍绑定旧 FR-A 编号；Demo 改造时统一迁移。 |

## 数据需求

- eSIM 商店入口、目的地搜索、目的地/区域/全球套餐曝光与点击。
- 套餐详情打开、覆盖范围打开、兼容性提示曝光与继续购买/取消购买。
- Stripe Checkout 创建、打开、取消、支付成功 Webhook、支付失败、退款。
- 履约下单请求、履约成功、履约失败、交付耗时、重复回调拦截。
- 我的 eSIM 列表/详情曝光、安装方式打开、安装指引失败、连接指引打开、状态刷新和用量刷新。
- Top up 资格返回、加购包曝光、创建结算、支付成功、履约成功/失败。
- 帮助入口、异常订单查看、退款申请、客服工单创建。
- 区分套餐 ID、目的地类型、内部订单 ID、履约渠道订单状态、支付状态、客户端版本、系统、设备兼容性结果和网络请求失败原因；不得上报完整 ICCID、二维码、SM-DP+ 地址或激活码。

## 待确认

1. HelloTalk 的首发售卖国家、支付币种、税费/折扣规则和客服承接团队。
2. 首期履约渠道合同实际提供的 API、Webhook、安装方式、用量能力、Top up 规则与退款能力；当前以 Airalo Partner Platform 签约账户文档和沙箱结果为参考，上线规则以最终合作渠道能力为准。
3. 设备兼容性采用 App 内型号查询、系统能力读取，还是跳转 HelloTalk 兼容设备页面。
4. 支付成功但履约失败的自动退款阈值、人工处理 SLA 和用户通知方式。
